# packages/domain

**Owner:** `ibpe-architecture`

Pure helpers (no I/O): track/domain mapping, bank key formatting.

Keep business rules here; leave persistence to `packages/database` and scrape I/O to `scrapers/`.
